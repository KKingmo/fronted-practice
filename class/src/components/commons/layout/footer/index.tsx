import styled from "@emotion/styled";

const Wrapper = styled.div`
  height: 50px;
  background-color: grey;
`;

export default function LayoutFooter() {
  return <Wrapper>여기는 푸터입니다 입니다.</Wrapper>;
}
