import styled from "@emotion/styled";

const Wrapper = styled.div`
  height: 50px;
  background-color: #f56060;
`;

export default function LayoutHeader() {
  return <Wrapper>Header Area</Wrapper>;
}
